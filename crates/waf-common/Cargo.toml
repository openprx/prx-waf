[package]
name = "waf-common"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
bytes = { workspace = true }
# Bounded MPSC transport for the cross-crate notification bus (`notify`).
# Already in the build graph via every other crate, so this adds no new
# dependency to the workspace.
tokio = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }
aes-gcm = { workspace = true }
sha2 = { workspace = true }
base64 = { workspace = true }
url = { workspace = true }
# Prometheus registry + text exposition for `metrics`. Only the encoder half is
# used; the counters are this crate's own lock-free tables.
prometheus-client = { workspace = true }
# Slow, salted KDF for encryption at rest (M-12)
argon2 = { workspace = true }
rand = { workspace = true }

[lints]
workspace = true
