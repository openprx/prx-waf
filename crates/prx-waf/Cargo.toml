[package]
name = "prx-waf"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[[bin]]
name = "prx-waf"
path = "src/main.rs"

[dependencies]
waf-common = { path = "../waf-common" }
waf-storage = { path = "../waf-storage" }
waf-engine = { path = "../waf-engine" }
waf-api = { path = "../waf-api" }
waf-cluster = { path = "../waf-cluster" }
gateway = { path = "../gateway" }

tokio = { workspace = true }
clap = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
uuid = { workspace = true }
ipnet = { workspace = true }
# `rules semantic --format json` emits the Lane 2 rule inventory for a script to
# diff. Already in the tree via waf-common/waf-engine, so this is a link, not a
# new supply-chain edge.
serde_json = { workspace = true }
rustls = { workspace = true }
nix = { workspace = true }

pingora-core = { workspace = true }
pingora-proxy = { workspace = true }

[dev-dependencies]
bytes = { workspace = true }

[lints]
workspace = true
